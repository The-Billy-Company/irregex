# Training evidence tests

These tests build only tiny synthetic packages in temporary directories. They
cover deterministic ranking, content-derived dataset fingerprints, held-out
call/session disjointness, sealed partitions, ZIP traversal and symlink
rejection, conservative Unicode handling, and the non-promotable output gate.
