The `relate` warm retrieval session (`recall.zig`) now guards its `search`/`pack` with the shared `Ward` reader/writer primitive instead of a plain `Io.Mutex`, so concurrent relate queries overlap under a shared lease on the watcher-clean fast path (only an overlay recompute takes the exclusive lease) — the same reader-overlap gist queries already had. Read safety is sound because the retrieval lane is read-only over the session's `persisted`/`fresh_ids`.
