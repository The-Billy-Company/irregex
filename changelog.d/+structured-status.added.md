`gist status --json` now exposes a versioned lifecycle snapshot for Python,
Rust, and agent consumers, derived from the same data model as the human report
so callers no longer need to parse status prose.
