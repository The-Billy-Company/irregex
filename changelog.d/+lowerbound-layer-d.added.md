Added **Layer D of the Certificate of Optimality — the algorithmic lower bound** (`bench/lowerbound/`): a fail-closed byte-touch audit proving gist sits at the information-theoretic floor, verified structurally against an independent single-pass reference on the real 160 MiB corpus — the fused byte-class DFA touches each candidate byte **exactly once** (`passes ≡ 1.0000`, no memchr-then-rescan double traffic; KMP'77 / Boyer-Moore'77 Ω(n) verify floor), the SIMD literal path reads **≤** the floor, and the trigram prefilter prunes as much as 95.65% of the corpus untouched before verify (Cox'12). 11/11 classes at the floor; the gate is proven non-tautological by fault injection (a single extra byte-read per line trips `exit 1`).
