Fixed a drift risk between Layer A (`certify.zig`) and Layer D (`lowerbound.zig`) of the Certificate of Optimality: the eleven probe-class definitions were hand-duplicated across the two files. Extracted them into a single shared `bench/harness/probes.zig` module both layers import, so the certificate's layers can no longer silently diverge. Also wired Layer C (roofline) and Layer D (lowerbound) into `build.zig` as `zig build roofline`/`zig build lowerbound` (previously unwired source with no executable target) and added the Layer B probe-vs-production differential test to `zig build test`.
