Register `billy-irregex` in the root uv workspace so AI development and tests
import the local binding reliably. Production AI deliberately omits the package,
binary, and proprietary repository corpus, making repository search unavailable
instead of searching an unrelated container filesystem.
