`--json` now disables index read-elision, exactly like `--stats` always has. The JSON summary message embeds the same `searches`/`bytes_searched` counters, so eliding index-cleared files under-reported both relative to ripgrep (which reads every walked file) — acceleration was changing observable output, caught by the multi-corpus sweep on CPython. With the gate added to `trigramFilter`, the full 472-case sweep (5 corpora × both engines) passes clean.
