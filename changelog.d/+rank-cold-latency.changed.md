Profiling the cold `--rank` path on a fat-candidate probe found 4.2 s hiding in two places, neither of them ranking. First, freshness: the macOS journal replay blocked ~1.9 s in `FSEventStreamFlushSync` before draining a single event — the flush is gone, the runloop drain now runs under an explicit budget (75 ms per query, 500 ms at daemon boot), a lost race writes a per-token `journal.skip` marker so later queries jump straight to the sweep walk, and `amend` re-mints the since-token exactly like a full build so the replay window stays "since the last amend" instead of growing forever. Second, feature extraction: `fileDoc` ran a full per-line pass over every candidate and only then consulted `docMatch` — inverted, one fused whole-buffer `docMatch` now rejects trigram false positives at the one-pass floor and only real matchers fund the per-line signals (also fixing a phantom-final-line overcount for `^$`-shaped patterns under rg's line model). Cold fat-probe rank drops 4.2 s → ~30 ms with a fresh index (~150 ms on a busy tree paying the bounded probe), set-equal with `gist -l` and def-boost/gen-demotion invariants intact.
