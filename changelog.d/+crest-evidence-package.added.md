CREST's production proof now fixes and pins the class-run optional-seam false negative against the real matcher, records all ASCII/Unicode × case-sensitive/caseless differentials, corpus identities, and every raw timing sample behind `crest.csv`, and ships a stdlib-only release-evidence packager. Clean-tree packages bind one Git revision, source archive, test and benchmark receipts, machine/cache metadata, and a `git show`-rendered monograph through fail-closed SHA-256 manifests, detached hashes, and exact path/byte/mode reproduction from the claimed Git object.
