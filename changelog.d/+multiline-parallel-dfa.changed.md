`-U` multiline now rides the parallel per-file pipeline instead of falling through to the serial engine, and an assertion-free multiline pattern (nothing positional to resolve — no `^ $ \b \A \z`) determinizes exactly, so `bufMatch` answers from the O(1)/byte byte-class DFA instead of a Pike re-seed per position; `-U -l` additionally short-circuits at the first kept span (`Emitter.buffer` files-only fast path). Both declared `-U` matrix losses flip to wins — `multiline-rare-files` 0.84x → ~3.5x and `multiline-common-lazy-dotstar-files` (`import \([\s\S]*?\)`, the table's deepest loss) 0.36x → ~2.8x — with byte-identical parity held by a new assertion-free-multiline differential fuzz lane against the Pike oracle.
