The freshness walk (`index/trigrams/fresh.zig`) no longer emits a file twice when its parent directory expands to children but no subdirectories: `buildWorkItems` re-queued such childless directories as leaf items after `expandOneLevel` had already emitted their files, double-visiting every file underneath. Surfaced by the kinship-atlas fold tests (a changed file re-sketched twice inflated the folded path count); fixed at the walk so every consumer of the changed-file report sees each path exactly once.
