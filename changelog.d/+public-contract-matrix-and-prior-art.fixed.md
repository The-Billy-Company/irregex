Gist's public claims now match its shipped surface: `--schema` renders a four-bucket ripgrep compatibility matrix from the parser's own declarative flag catalog, including ASCII-only `-i`/`\b`/`\w` differences and fail-loud exclusions; the C ABI is documented and gated as the existing two-symbol primitive surface, with a real C compile/link/run smoke in `zig build test`; and `PRIOR_ART.md` distinguishes Gist's agent-workload composition from established indexed, semantic, and structural code-search systems.
