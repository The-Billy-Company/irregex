Added Layer C (roofline) of the optimality certificate under bench/roofline/: a zero-dependency STREAM-style read-bandwidth microbenchmark (bandwidth.zig) that measures this machine's single-core memory ceiling at L1/L2/DRAM tiers and gist's own SIMD scan bandwidth over the corpus, plus roofline_report.py which splices a `## Layer C — roofline (hardware ceiling)` section into CERTIFICATE.md. Proves gist's scan is memory-bandwidth-bound — attaining a large fraction of the pure-read ceiling on this M4 Max (measured, e.g. L1 126 / L2 76 / DRAM 72 GB/s under load; gist pure-streaming scan ~28% of DRAM ceiling as a same-run ratio), so the real win is the trigram prefilter keeping bytes away from the scan.
