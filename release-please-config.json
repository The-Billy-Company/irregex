{
  "$schema": "https://raw.githubusercontent.com/googleapis/release-please/main/schemas/config.json",

  "_comment": [
    "Conventional Commits → a version bump, a tag, and a GitHub Release.",
    "",
    "── One package, one version",
    "This repository ships one thing four ways: the Zig module, `libirgx` +",
    "include/irgx.h, and the Python / Rust / Go bindings. They are released",
    "together, so they carry one number, and `build.zig.zon`'s `.version` is",
    "where that number is written. Zig reads it through a build option, Rust",
    "reads CARGO_PKG_VERSION, and Python reads its installed distribution",
    "metadata — none of them restate it. What remains is the packaging",
    "manifests that cannot import anything, listed as `extra-files` below.",
    "",
    "── Why `simple` + generic extra-files",
    "There is no Zig updater, and the version is pinned in lockstep across",
    "files four languages own. `simple` keeps the manifest as the source of",
    "truth and the `generic` updater rewrites the semver on any line carrying",
    "an `x-release-please-version` annotation, so one release moves every",
    "member of the lockstep atomically. `tools/version_parity.py` fails if a",
    "marked line is missing from this list, so the two cannot drift apart.",
    "",
    "── Changelog ownership",
    "towncrier owns CHANGELOG.md — the curated fragments under changelog.d/",
    "are the release notes, written for people. release-please would compile a",
    "second, machine-derived changelog into the same file and fight it, so",
    "`skip-changelog` is on and the bot handles versions and tags only. The",
    "`fold changelog` job in release-please.yml builds the notes and commits",
    "them onto this PR's branch automatically; release.yml's preflight",
    "re-checks that the fold landed before it will publish anything.",
    "",
    "── What picks the number",
    "The stock `default` strategy, unmodified: a `!` or a BREAKING CHANGE",
    "footer takes the major, `feat` takes the minor, and everything else takes",
    "the patch. `bump-minor-pre-major` and `bump-patch-for-minor-pre-major`",
    "used to sit below and are gone — release-please reads them only while the",
    "version is under 1.0.0, so they went inert when this package shipped",
    "1.0.0 and stayed on reading like a policy nobody consults. The full table,",
    "the 0.x variant, and the `Release-As: X.Y.Z` override that pins an exact",
    "version live in the org standard:",
    "https://github.com/The-Billy-Company/.github/blob/main/RELEASING.md#what-picks-the-number",
    "",
    "── No `package-name`",
    "It used to say `irregex`, and that is what kept the bot from ever cutting",
    "a release. With the component out of the tag, release-please writes the",
    "release PR's body with no component and names its branch",
    "`release-please--branches--main` with no component either; but the guard it",
    "then runs over a merged standalone PR compares that empty component",
    "against `component || package-name`. A name here makes those two disagree",
    "forever, so every merge logged `PR component: undefined does not match",
    "configured component: irregex` and returned without tagging — which then",
    "reads as an untagged merged release PR and blocks the NEXT run too. The",
    "name buys nothing back: with `include-component-in-tag` false, the tag is",
    "`vX.Y.Z` and the release is titled `vX.Y.Z` whatever the package is",
    "called. v2.1.0 had to be tagged and published by hand because of this.",
    "",
    "── The tag cascades",
    "Merging the release PR tags `vX.Y.Z`, which is what `release.yml` waits",
    "for to publish the wheels. A tag pushed by GITHUB_TOKEN does NOT trigger",
    "another workflow, so that hand-off only happens because release-please runs",
    "as the `billy-company-release` GitHub App rather than as Actions — see",
    ".github/workflows/release-please.yml."
  ],

  "release-type": "simple",
  "include-component-in-tag": false,
  "include-v-in-tag": true,
  "separate-pull-requests": false,
  "draft": false,
  "prerelease": false,

  "changelog-sections": [
    { "type": "feat", "section": "Features" },
    { "type": "fix", "section": "Bug Fixes" },
    { "type": "perf", "section": "Performance" },
    { "type": "refactor", "section": "Refactor" },
    { "type": "deps", "section": "Dependencies" },
    { "type": "revert", "section": "Reverts" },
    { "type": "docs", "section": "Documentation", "hidden": true },
    { "type": "test", "section": "Tests", "hidden": true },
    { "type": "build", "section": "Build", "hidden": true },
    { "type": "ci", "section": "CI", "hidden": true },
    { "type": "chore", "section": "Chore", "hidden": true },
    { "type": "style", "section": "Style", "hidden": true }
  ],

  "packages": {
    ".": {
      "release-type": "simple",
      "skip-changelog": true,
      "extra-files": [
        { "type": "generic", "path": "build.zig.zon" },
        { "type": "generic", "path": "contract/engine.toml" },
        { "type": "generic", "path": "bindings/python/pyproject.toml" },
        { "type": "generic", "path": "bindings/python/irgx/contract/abi.py" },
        { "type": "generic", "path": "bindings/rust/Cargo.toml" },
        { "type": "generic", "path": "bindings/go/analytic/contract.go" }
      ]
    }
  }
}
