# git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# Add full commit SHAs for mass-format or migration commits below.
# These revisions disappear from blame without disappearing from history.

# style: ruff formats the bench, binding, and tooling python
b0ac27ec5b5f6aa5634d9206c35a484b366ffcae

# refactor: the kernel package is now irregex end-to-end
# The rename sweep: 87% of its replaced lines are one bare token becoming
# another. Two lines in the tree still blame to it, which is the whole benefit -
# an audit of this history found the renames and reflows had already been
# overwritten by authored work, so there is very little left here to hide.
# The 169-@import repoint (f3331694) is the same shape at 99%
# and is deliberately *not* listed: zero lines blame to it today, and a blame
# count only falls as files change, so the entry could never earn its keep.
ee424e21ccd5a471440eae78e660273cb93c7a54
